name: weater_app_flutter
description: A meta é criarmos uma página simples, que consuma a API do WeatherAPI de previsão do temp.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  #B
  flutter_bloc: ^8.1.3
  
  #C
  cupertino_icons: ^1.0.2
  
  #D
  dio: ^5.3.2

  #F
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - .env

